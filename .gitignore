__pycache__/
logs/
*.log
.codex/
.agents/
*.bak
*.egg-info/
.vscode/

# All downloaded categories live under library/ (downloaded .txt +
# metadata.jsonl + _catalog.jsonl per category). The whole tree is local data.
/library/

# Embedding index - regenerable via `python recommend.py update`.
data/rec_index/

# Personal reading bookmarks (read.py).
data/reading_progress.json

# Reader-app runtime auth/state: secret + password hashes + per-user progress +
# admin job registry. Never commit these.
data/.jwt_secret
data/users.json
data/user_progress/
data/user_settings/
data/admin_jobs.json
data/admin_command_history.json

library/
reports/

CLAUDE.md