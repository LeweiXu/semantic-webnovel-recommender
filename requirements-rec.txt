# Recommendation-system dependencies — heavy; install into a DEDICATED venv,
# e.g.  python3 -m venv ~/venvs/recsys  (the scraper itself only needs
# requirements.txt). Run the tool with ~/venvs/recsys/bin/python recommend.py …
#
# PyTorch must match the GPU. For RTX 50-series (Blackwell, sm_120) install the
# CUDA 12.8 build FIRST from the PyTorch index, otherwise the generic wheel will
# lack sm_120 kernels:
#   ~/venvs/recsys/bin/pip install torch --index-url https://download.pytorch.org/whl/cu128
torch

# Embeddings + local LLM (rerank / query-parse / explain)
sentence-transformers
transformers>=4.45
accelerate

# Numerics
numpy

# Reserved for the optional lexical/BM25 hybrid signal (not required by the
# current embedding+tags pipeline)
jieba
rank-bm25

# Reused from the scraper for metadata extraction (recsys/extract.py imports
# scraper.source_url_from_file)
beautifulsoup4
curl-cffi
lxml
